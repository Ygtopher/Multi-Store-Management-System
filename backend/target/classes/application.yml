server:
  port: 8080

spring:
  application:
    name: store-management-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/store_management
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
